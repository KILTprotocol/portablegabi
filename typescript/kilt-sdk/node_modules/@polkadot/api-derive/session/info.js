"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.info = info;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _types = require("@polkadot/types");

var _util = require("../util");

// Copyright 2017-2019 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
// internal helper to just split the logic - take all inputs, do the calculations and combine
function createDerivedV1(_ref) {
  let [bestNumber, {
    currentIndex,
    validatorCount
  }, [_lastLengthChange, sessionLength, lastEraLengthChange, sessionsPerEra]] = _ref;
  const lastLengthChange = (_lastLengthChange === null || _lastLengthChange === void 0 ? void 0 : _lastLengthChange.unwrapOr(null)) || (0, _types.createType)('BlockNumber');
  const sessionProgress = bestNumber.sub(lastLengthChange).add(sessionLength).mod(sessionLength);
  const currentEra = currentIndex.sub(lastEraLengthChange).mod(sessionsPerEra);
  const eraProgress = currentEra.mul(sessionLength).add(sessionProgress);
  return {
    currentEra: (0, _types.createType)('EraIndex', currentEra),
    currentIndex,
    eraLength: (0, _types.createType)('BlockNumber', sessionLength.mul(sessionsPerEra)),
    eraProgress: (0, _types.createType)('BlockNumber', eraProgress),
    isEpoch: false,
    lastEraLengthChange,
    lastLengthChange,
    sessionLength,
    sessionsPerEra,
    sessionProgress: (0, _types.createType)('BlockNumber', sessionProgress),
    validatorCount
  };
}

function createDerivedLatest(_ref2) {
  let [[hasBabe, epochDuration, sessionsPerEra], {
    currentIndex,
    currentEra,
    validatorCount
  }, [currentSlot, epochIndex, epochOrGenesisStartSlot, currentEraStartSessionIndex]] = _ref2;
  const epochStartSlot = epochIndex.mul(epochDuration).add(epochOrGenesisStartSlot);
  const sessionProgress = currentSlot.sub(epochStartSlot);
  const eraProgress = currentIndex.sub(currentEraStartSessionIndex).add(sessionProgress); // console.log(sessionProgress);

  return {
    currentEra,
    currentIndex,
    eraLength: (0, _types.createType)('BlockNumber', sessionsPerEra.mul(epochDuration)),
    eraProgress: (0, _types.createType)('BlockNumber', eraProgress),
    isEpoch: hasBabe,
    lastEraLengthChange: (0, _types.createType)('BlockNumber'),
    lastLengthChange: (0, _types.createType)('BlockNumber', epochStartSlot),
    sessionLength: epochDuration,
    sessionsPerEra,
    sessionProgress: (0, _types.createType)('BlockNumber', sessionProgress),
    validatorCount
  };
}

function infoV1(api) {
  return (0, _rxjs.combineLatest)([api.derive.chain.bestNumber(), api.derive.session.indexes(), api.queryMulti([api.query.session.lastLengthChange, api.query.session.sessionLength, api.query.staking.lastEraLengthChange, api.query.staking.sessionsPerEra])]).pipe((0, _operators.map)(createDerivedV1));
}

function infoLatestAura(api) {
  return api.derive.session.indexes().pipe((0, _operators.map)(indexes => createDerivedLatest([[false, (0, _types.createType)('u64', 1), api.consts.staking.sessionsPerEra], indexes, [(0, _types.createType)('u64', 1), (0, _types.createType)('u64', 1), (0, _types.createType)('u64', 1), (0, _types.createType)('SessionIndex', 1)]])));
}

function infoLatestBabe(api) {
  return (0, _rxjs.combineLatest)([api.derive.session.indexes(), api.queryMulti([api.query.babe.currentSlot, api.query.babe.epochIndex, api.query.babe.genesisSlot, api.query.staking.currentEraStartSessionIndex])]).pipe((0, _operators.map)((_ref3) => {
    let [indexes, slots] = _ref3;
    return createDerivedLatest([[true, api.consts.babe.epochDuration, api.consts.staking.sessionsPerEra], indexes, slots]);
  }));
}
/**
 * @description Retrieves all the session and era info and calculates specific values on it as the length of the session and eras
 */


function info(api) {
  const query = api.consts.staking ? api.consts.babe ? infoLatestBabe // 2.x with Babe
  : infoLatestAura // 2.x with Aura (not all info there)
  : infoV1;
  return () => query(api).pipe((0, _util.drr)());
}